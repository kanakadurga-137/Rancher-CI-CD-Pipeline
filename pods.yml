apiVersion: v1
kind: Pod
metadata:
  name: test1
spec:
  replicas: 1
  selector:
    matchLabels:
      app: test1
  template:
    metadata:
      labels:
        app: test1
    spec:
      imagePullSecrets:
      - name: docksec
      containers:
      - name: testa
        imagePullPolicy: IfNotPresent
        image: mbhaskar2005/firstdockerrepo:tagVersion
        ports:
        - containerPort: 8080
